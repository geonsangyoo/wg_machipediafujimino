import Colors from './Colors';

export default {
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: Colors.defaultBackgroundColor
    },
    labelStyle: {
        fontSize: 12,
        fontFamily: "meiryoUI",
        fontWeight: "bold"
    }
};